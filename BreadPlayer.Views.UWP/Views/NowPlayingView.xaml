<Page
    x:Class="BreadPlayer.NowPlayingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BreadPlayer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.Background>
            <ImageBrush Stretch="UniformToFill"/>
        </Grid.Background>
        <Rectangle Fill="#99000000" Grid.ColumnSpan="3"/>
        <StackPanel Grid.Column="0" Background="Gray" Grid.ColumnSpan="2" Margin="0,0,-50,0" Orientation="Horizontal" VerticalAlignment="Bottom">
            <Image Source="ms-appx:///Assets/albumart.png"  Margin="-60,0,0,0" VerticalAlignment="Bottom" Height="150" Width="150"/>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Typography.Capitals="AllSmallCaps" Text="Hello" FontSize="18" FontWeight="ExtraLight"/>
                <TextBlock Text="Adele" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="25" FontSize="18" FontWeight="Bold"/>
            </StackPanel>
        </StackPanel>
        <ListView x:Name="playcontrolsListView" Grid.Column="2" Width="50" HorizontalAlignment="Right" Background="{ThemeResource SystemControlBackgroundAccentBrush}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ToolTipService.ToolTip="{Binding Path=Tooltip}">                       
                        <FontIcon FontSize="20" Glyph="{Binding FontGlyph}" Margin="4,0,0,0" VerticalAlignment="Center"
                      HorizontalAlignment="Center" FontWeight="SemiLight" FontFamily="Segoe MDL2 Assets"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsStackPanel VerticalAlignment="Bottom"></ItemsStackPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
       
    </Grid>
</Page>

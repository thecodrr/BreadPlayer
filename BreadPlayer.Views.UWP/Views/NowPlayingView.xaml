<Page
    x:Class="BreadPlayer.NowPlayingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BreadPlayer"
    xmlns:VM="using:BreadPlayer.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"   
    mc:Ignorable="d">
    <Page.Resources>
        <VM:NowPlayingViewModel x:Key="NowPlayingVM"/>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Rectangle Fill="{StaticResource ThemeBackground}" Grid.ColumnSpan="3" Grid.RowSpan="5"/>
        <Grid DataContext="{StaticResource Core}" Margin="50,0,0,0" Grid.Row="1" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Image Width="150" Height="150" Grid.RowSpan="3" Source="ms-appx:///Assets/Noise.jpg">
            </Image>
            <TextBlock Margin="20,0,0,0" Grid.Column="1" Grid.Row="0" Typography.Capitals="AllSmallCaps" TextWrapping="Wrap" Text="Hello" FontSize="44" FontWeight="ExtraLight"/>
            <TextBlock Margin="20,0,0,0" Grid.Column="1" Grid.Row="1" Typography.Capitals="AllSmallCaps" Text="Adele" FontSize="34" FontWeight="ExtraLight"/>
            <TextBlock Margin="20,0,0,0" Grid.Column="1" Grid.Row="2" Typography.Capitals="AllSmallCaps" Text="25" FontSize="24" FontWeight="ExtraLight"/>
        </Grid>
        <StackPanel x:Name="playcontrolsPanel" Grid.Row="2" Grid.ColumnSpan="2" VerticalAlignment="Top" DataContext="{StaticResource ShellVM}" Orientation="Horizontal" HorizontalAlignment="Center">
            <AppBarButton Foreground="{StaticResource ThemeForeground}" Icon="Previous" Command="{Binding PlayPreviousCommand}" Style="{StaticResource CustomAppBarButtonStyle}" VerticalAlignment="Stretch"/>
            <AppBarButton Foreground="{StaticResource ThemeForeground}" Height="60" Command="{Binding PlayPauseCommand}" VerticalAlignment="Stretch"  Style="{StaticResource CustomAppBarButtonStyle}">
                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE768;" FontSize="34"/>
            </AppBarButton>
            <AppBarButton Foreground="{StaticResource ThemeForeground}" Icon="Next" Command="{Binding PlayNextCommand}" Style="{StaticResource CustomAppBarButtonStyle}" VerticalAlignment="Stretch"/>
        </StackPanel>
        <StackPanel Height="50" Margin="0,0,0,0" x:Name="extrasPanel" Grid.ColumnSpan="2" Grid.Row="3" VerticalAlignment="Top" DataContext="{StaticResource ShellVM}" Orientation="Horizontal" HorizontalAlignment="Center">
            <AppBarToggleButton Foreground="{StaticResource ThemeForeground}" Icon="Shuffle" Style="{StaticResource CustomAppBarToggleButtonStyle}" VerticalAlignment="Stretch"/>
            <AppBarToggleButton Foreground="{StaticResource ThemeForeground}" Style="{StaticResource CustomAppBarToggleButtonStyle}" VerticalAlignment="Stretch" Content="&#x04c;" FontSize="20" FontFamily="Assets/breadplayer.ttf#breadplayer"/>
            <AppBarToggleButton Foreground="{StaticResource ThemeForeground}" Icon="Volume" Style="{StaticResource CustomAppBarToggleButtonStyle}" VerticalAlignment="Stretch"/>
            <AppBarButton Foreground="{StaticResource ThemeForeground}" Icon="RepeatAll" Style="{StaticResource CustomAppBarButtonStyle}" VerticalAlignment="Stretch"/>
            <AppBarButton Foreground="{StaticResource ThemeForeground}" Icon="OpenFile" Command="{Binding OpenSongLocationCommand}" Style="{StaticResource CustomAppBarButtonStyle}" VerticalAlignment="Stretch"/>
        </StackPanel>
        <Pivot Title="Related Information" DataContext="{StaticResource NowPlayingVM}" Margin="50,50,0,0">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontWeight="Bold"/>
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <PivotItem Header="Artist Biography">
                <TextBlock Text="{Binding Path=ArtistBio}" TextWrapping="WrapWholeWords" TextAlignment="Justify"/>
            </PivotItem>
            <PivotItem Header="Album Information">
                <ListView ItemsSource="{Binding Path=AlbumTracks, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name"></ListView>
            </PivotItem>           
        </Pivot>

        <ListView Header="Lyrics" Margin="50,50,50,0" Grid.Column="1" ItemsSource="{Binding Path=NowPlayingQueue}">
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontWeight="Bold" Margin="10,20,0,0"/>
                </DataTemplate>
            </ListView.HeaderTemplate>
        </ListView>
    </Grid>
</Page>

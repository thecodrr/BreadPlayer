<Page
    x:Class="BreadPlayer.NowPlayingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BreadPlayer"
    xmlns:VM="using:BreadPlayer.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:BPControls="using:BreadPlayer.Controls"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    mc:Ignorable="d">
    <Page.Resources>
        <VM:NowPlayingViewModel x:Key="NowPlayingVM"/>
    </Page.Resources>
    <Grid DataContext="{StaticResource NowPlayingVM}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Rectangle Grid.ColumnSpan="3" Grid.RowSpan="10" Fill="#FFEBEBEB" Opacity="0.9"/>
        <Grid DataContext="{Binding Source={StaticResource Core}, Path=Player.CurrentlyPlayingFile}" Margin="50,0,0,0" Grid.Row="1" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Image Width="150" Height="150" Grid.RowSpan="3" Source="{Binding Path=AttachedPicture}">
            </Image>
            <TextBlock Margin="20,0,0,0" Grid.Column="1" Grid.Row="0" Typography.Capitals="AllSmallCaps" TextWrapping="Wrap" Text="{Binding Path=Title}" FontSize="44" FontWeight="ExtraLight"/>
            <TextBlock Margin="20,0,0,0" Grid.Column="1" Grid.Row="1" Typography.Capitals="AllSmallCaps" Text="{Binding Path=LeadArtist}" FontSize="34" FontWeight="ExtraLight"/>
            <TextBlock Margin="20,0,0,0" Grid.Column="1" Grid.Row="2" Typography.Capitals="AllSmallCaps" Text="{Binding Path=Album}" FontSize="24" FontWeight="ExtraLight"/>
        </Grid>
        <StackPanel x:Name="playcontrolsPanel" Grid.Row="2" Grid.ColumnSpan="2" VerticalAlignment="Top" DataContext="{StaticResource ShellVM}" Orientation="Horizontal" HorizontalAlignment="Center">
            <AppBarButton Foreground="{StaticResource ThemeForeground}" Icon="Previous" Command="{Binding PlayPreviousCommand}" Style="{StaticResource CustomAppBarButtonStyle}" VerticalAlignment="Stretch"/>
            <AppBarButton Foreground="{StaticResource ThemeForeground}" Height="60" Command="{Binding PlayPauseCommand}" VerticalAlignment="Stretch"  Style="{StaticResource CustomAppBarButtonStyle}">
                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE768;" FontSize="34"/>
            </AppBarButton>
            <AppBarButton Foreground="{StaticResource ThemeForeground}" Icon="Next" Command="{Binding PlayNextCommand}" Style="{StaticResource CustomAppBarButtonStyle}" VerticalAlignment="Stretch"/>
        </StackPanel>
        <StackPanel Height="50" Margin="0,0,0,20" x:Name="extrasPanel" Grid.ColumnSpan="2" Grid.Row="3" VerticalAlignment="Top" DataContext="{StaticResource ShellVM}" Orientation="Horizontal" HorizontalAlignment="Center">
            <AppBarToggleButton Foreground="{StaticResource ThemeForeground}" Icon="Shuffle" Style="{StaticResource CustomAppBarToggleButtonStyle}" VerticalAlignment="Stretch"/>
            <AppBarToggleButton Foreground="{StaticResource ThemeForeground}" Style="{StaticResource CustomAppBarToggleButtonStyle}" VerticalAlignment="Stretch" Content="&#x04c;" FontSize="20" FontFamily="Assets/breadplayer.ttf#breadplayer"/>
            <AppBarToggleButton Foreground="{StaticResource ThemeForeground}" Icon="Volume" Style="{StaticResource CustomAppBarToggleButtonStyle}" VerticalAlignment="Stretch"/>
            <AppBarButton Foreground="{StaticResource ThemeForeground}" Icon="RepeatAll" Style="{StaticResource CustomAppBarButtonStyle}" VerticalAlignment="Stretch"/>
            <AppBarButton Foreground="{StaticResource ThemeForeground}" Icon="OpenFile" Command="{Binding OpenSongLocationCommand}" Style="{StaticResource CustomAppBarButtonStyle}" VerticalAlignment="Stretch"/>
        </StackPanel>
        <Pivot Title="Related Information" Grid.ColumnSpan="3" HorizontalAlignment="Center" DataContext="{StaticResource NowPlayingVM}" Margin="50,50,50,10" Style="{StaticResource CustomPivot}">
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontWeight="Bold"/>
                </DataTemplate>
            </Pivot.HeaderTemplate>
            <PivotItem Header="Now Playing">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ListView Grid.Column="0" DataContext="{StaticResource ShellVM}" ItemsSource="{Binding Path=NowPlayingQueue, UpdateSourceTrigger=Explicit}">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="PlayIcon" Text="&#xE768;" FontSize="20" VerticalAlignment="Center" FontFamily="Segoe MDL2 Assets" Visibility="{Binding Path=State, Converter={StaticResource PlayerStateToVisibility}}"/>
                                    <TextBlock x:Name="Title" Grid.Column="1" Margin="10,0,0,0" Text="{Binding Path=Title}" VerticalAlignment="Center"/>
                                    <Border Grid.ColumnSpan="3" Background="Transparent" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ToolTipService.ToolTip="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content.Title}">
                                        <Interactivity:Interaction.Behaviors>
                                            <Core:EventTriggerBehavior EventName="DoubleTapped">
                                                <Core:InvokeCommandAction CommandParameter="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}, Path=Content}" Command="{Binding Source={StaticResource LibVM},Path=PlayCommand}"></Core:InvokeCommandAction>
                                            </Core:EventTriggerBehavior>                                           
                                            <Core:EventTriggerBehavior EventName="Tapped">
                                                <Core:CallMethodAction MethodName="PlayOnTap" TargetObject="{StaticResource LibVM}"></Core:CallMethodAction>
                                            </Core:EventTriggerBehavior>
                                            <Core:DataTriggerBehavior Binding="{Binding State, Converter={StaticResource PlayerStateToBool}}" Value="True">
                                                <Core:ChangePropertyAction PropertyName="Foreground" TargetObject="{Binding ElementName=Title}" Value="{ThemeResource PlaybarBrush}"/>
                                                <Core:ChangePropertyAction PropertyName="Foreground" TargetObject="{Binding ElementName=PlayIcon}" Value="{ThemeResource PlaybarBrush}"/>
                                            </Core:DataTriggerBehavior>
                                            <Core:DataTriggerBehavior Binding="{Binding State, Converter={StaticResource PlayerStateToBool}}" Value="False">
                                                <Core:ChangePropertyAction PropertyName="Foreground" TargetObject="{Binding ElementName=Title}" Value="Black"/>
                                                <Core:ChangePropertyAction PropertyName="Foreground" TargetObject="{Binding ElementName=PlayIcon}" Value="Black"/>
                                            </Core:DataTriggerBehavior>
                                        </Interactivity:Interaction.Behaviors>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>                   
                </Grid>
            </PivotItem>
            <PivotItem Header="Artist Biography">
                <Grid>
                    <ScrollViewer>
                        <TextBlock Text="{Binding Path=ArtistBio}" TextWrapping="WrapWholeWords" TextAlignment="Justify"/>
                    </ScrollViewer>
                    <ProgressRing Width="50" Height="50" IsActive="{Binding Path=IsLoading}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </PivotItem>
            <PivotItem Header="Album Information">
                <ListView ScrollViewer.VerticalScrollBarVisibility="Visible" ItemsSource="{Binding Path=AlbumTracks, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name"></ListView>
            </PivotItem>           
        </Pivot>
      
        <StackPanel x:Name="MetaCorrectionPanel" CornerRadius="20" Margin="50,100,0,0" Visibility="{Binding Path=IsFailed, Converter={StaticResource BoolToVisibility}, FallbackValue=Collapsed}" Padding="50" Background="{ThemeResource BackgroundOverlay}" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBox Padding="14,6,14,8" PlaceholderText="Enter the correct song artist here..." Style="{StaticResource RoundedTextBoxStyle}"/>
            <TextBox Padding="14,6,14,8" PlaceholderText="Enter the correct song album here..." Margin="0,10,0,0" Style="{StaticResource RoundedTextBoxStyle}"/>
            <Button Content="Retry" Padding="50,10,50,10" HorizontalAlignment="Center" Margin="0,10,0,0" Style="{StaticResource RoundedIconButtonStyle}"/>
        </StackPanel>
      
    </Grid>
</Page>

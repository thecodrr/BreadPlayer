<Page
    x:Class="Macalifa.Albums"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Macalifa"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="53*"/>
            <RowDefinition Height="267*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="a" Text="ALBUMS!!!!!!!!!!!!!" FontSize="72" Margin="0,0,0,9.667"/>
        <ListBox x:Name="playListBox" Margin="0,0,0,60" Height="629" VerticalAlignment="Bottom" Grid.Row="1" ItemContainerStyle="{StaticResource CustomListItemStyle}"></ListBox>
    </Grid>
</Page>
